import { TsFileUploadComponent } from './file-upload.component';


describe(`TsFileUploadComponent`, () => {
  let component: TsFileUploadComponent;

  beforeEach(() => {
    component = new TsFileUploadComponent();
  });


  test(`should exist`, () => {
    expect(component).toBeTruthy();
  });

});
